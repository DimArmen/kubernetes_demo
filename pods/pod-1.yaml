apiVersion: v1
kind: Pod
metadata:
  name: web-1
  labels:
    app: web-1
spec:
  containers:
    - name: web
      image: nginx:latest
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
