apiVersion: v1
kind: Pod
metadata:
  name: web-2
  labels:
    app: web-2
spec:
  containers:
    - name: web
      image: nginx:latest
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
